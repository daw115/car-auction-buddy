# Status migracji GUI Supabase → Ubuntu API

Ten dokument jest **żywą tabelą stanu**. Aktualizuj go przy każdej zmianie źródła danych ekranu.

**Stan bieżący (etap 2 — transport):** żaden ekran nie został przełączony na Ubuntu jako _źródło danych_. Runtime GUI nadal korzysta z Supabase dla danych, ale **warstwa transportu** dla wywołań FastAPI (core/jobs/records/cache/settings) została ujednolicona w `src/server/backend-transport.server.ts`. Wybór między Ubuntu API a legacy `API_BASE_URL` odbywa się na podstawie envów — bez runtime fallbacku po wybraniu Ubuntu. Stan endpointów: **`transport-ready, schema-unverified`**.

- **Rollback transportu**: usuń wszystkie cztery envy Ubuntu (`UBUNTU_API_BASE_URL`, `UBUNTU_API_BEARER_TOKEN`, `CF_ACCESS_CLIENT_ID`, `CF_ACCESS_CLIENT_SECRET`) — transport wraca do legacy `API_BASE_URL`/`API_BEARER_TOKEN`.
- **Częściowa konfiguracja Ubuntu ≠ rollback**: dowolny podzbiór czterech envów Ubuntu ⇒ fail closed z sanitized error „Incomplete Ubuntu API configuration". Legacy nie jest wtedy używany.
- **Supabase** pozostaje legacy runtime dla wszystkich ekranów danych (records, watchlist, watch queue, dashboard, PasswordGate, operation_logs, ustawienia backed przez tabele).
- **GUI/komponenty**: bez zmian. Migracja transportu jest niewidoczna dla klienta.

## Legenda

- **Źródło (obecne)**: `supabase` = tabela w Supabase; `legacy-api` = wywołanie do `API_BASE_URL`; `mixed` = jedno i drugie.
- **Endpoint (docelowy)**: patrz `docs/ubuntu-api-contract.md`.
- **Status endpointu**: `verified-existing` / `required-unverified` / `missing` / `blocked-by-backend-discovery`.
- **Rollback**: jak wrócić do legacy, jeżeli migracja się nie uda.

## Ekrany

| Ekran / plik                                                    | Źródło (obecne)                                          | Endpoint (docelowy)                                                                           | Status endpointu                                       | Blocker migracji                                                                                            | Rollback                                                        |
| --------------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| PasswordGate (`src/components/PasswordGate.tsx`)                | supabase (`site_user_passwords`)                         | `/auth/login`, `/auth/logout`, `/auth/session`, `/auth/users/{u}/password`, `/auth/users/{u}` | `blocked-by-backend-discovery`                         | Ubuntu nie ma jeszcze endpointów auth. Zamiana bez nich = brak logowania do GUI.                            | Nie migrować dopóki endpointy nie są `verified-existing`.       |
| `/` — panel wyszukiwania (`src/routes/index.tsx`)               | mixed (supabase logi + legacy-api scraper)               | `/api/search`, `/api/search/batch`, `/api/jobs/*`, `/audit/search`                            | `required-unverified` / `blocked-by-backend-discovery` | Discovery kontraktu `/audit/*`. Format statusów job pokrywa się z obecnym legacy — do potwierdzenia parity. | Wyłączyć nowy kod, wrócić do legacy proxy `backendSearchBatch`. |
| `/dashboard` (`src/routes/dashboard.tsx`)                       | supabase                                                 | `/records`, `/audit/operations`                                                               | `blocked-by-backend-discovery`                         | Brak potwierdzonych schematów list rekordów i logów operacyjnych na Ubuntu.                                 | Zostawić na Supabase.                                           |
| `/database` (`src/routes/database.tsx`)                         | supabase                                                 | n/a (podgląd Supabase)                                                                        | `missing`                                              | Ekran technicznie zależny od Supabase. Do decyzji, czy w ogóle migrować.                                    | Nie migrować.                                                   |
| `/records` (`src/routes/records.tsx`)                           | supabase (`records`, `clients`)                          | `/records`, `/clients`                                                                        | `blocked-by-backend-discovery`                         | Discovery `/records`, `/clients` na Ubuntu.                                                                 | Zostawić na Supabase.                                           |
| `/watchlist` (`src/routes/watchlist.tsx`)                       | supabase (`watchlist_items`)                             | `/watchlist`                                                                                  | `blocked-by-backend-discovery`                         | Discovery watchlist na Ubuntu.                                                                              | Zostawić na Supabase.                                           |
| Watch queue (dialog `no-results-queue-dialog.tsx`)              | supabase (`watch_queue`)                                 | `/queue`                                                                                      | `blocked-by-backend-discovery`                         | Discovery queue na Ubuntu.                                                                                  | Zostawić na Supabase.                                           |
| `/settings/ai` (`src/routes/settings.ai.tsx`)                   | supabase (`ai_provider_settings`)                        | `/api/settings/ai-providers`                                                                  | `required-unverified`                                  | Ubuntu musi potwierdzić schema (provider + model + override).                                               | Zostawić na Supabase.                                           |
| `/settings/filters` (`src/routes/settings.filters.tsx`)         | supabase (`pipeline_filters`)                            | `/api/settings/pipeline-filters`                                                              | `required-unverified`                                  | Discovery schematu (seller_types, body_types, auction window).                                              | Zostawić na Supabase.                                           |
| `/settings/default-criteria`                                    | supabase (`default_search_criteria`)                     | `/api/settings/default-criteria`                                                              | `required-unverified`                                  | Discovery kontraktu whole-object PUT.                                                                       | Zostawić na Supabase.                                           |
| `/settings/diagnostics` (`src/routes/settings.diagnostics.tsx`) | mixed (supabase envs + legacy-api `/health` + `/config`) | `/diagnostics` + `/health` (Ubuntu)                                                           | `blocked-by-backend-discovery`                         | Endpoint `/diagnostics` na Ubuntu nie istnieje. Sekcja Ubuntu API zasilana probem (`probeUbuntuApi`).       | Sekcja Supabase pozostaje niezmieniona.                         |
| `/api/health` (`src/routes/api/health.ts`)                      | mixed                                                    | `/health` (Ubuntu) — jako **dodatkowa** sekcja `services.ubuntuApi`                           | `required-unverified` (probe)                          | Ubuntu API traktowane jako **opcjonalne**. Niedostępność ≠ 503 GUI.                                         | Usunięcie envów `UBUNTU_*` → `unconfigured`.                    |
| `/api/config`                                                   | supabase (`app_config`)                                  | `/config`                                                                                     | `required-unverified`                                  | Discovery schematu app_config na Ubuntu.                                                                    | Zostawić na Supabase.                                           |
| `/api/records`                                                  | supabase                                                 | `/records`                                                                                    | `blocked-by-backend-discovery`                         | Discovery.                                                                                                  | Zostawić na Supabase.                                           |
| `/api/reports/pdf`                                              | server-side render (`@react-pdf/renderer`) + supabase    | `/reports/pdf`                                                                                | `blocked-by-backend-discovery`                         | Decyzja: czy PDF generuje Ubuntu, czy dalej BFF (obecnie BFF). Brak blockera dopóki BFF radzi sobie sam.    | Nie migrować w tej iteracji.                                    |
| `dev-logs` (`src/routes/dev.logs.tsx`)                          | supabase (`operation_logs`)                              | `/audit/operations`                                                                           | `blocked-by-backend-discovery`                         | Discovery formatu logów Ubuntu.                                                                             | Zostawić na Supabase.                                           |
| Batch polling (`src/hooks/use-batch-jobs-polling.ts`)           | legacy-api                                               | `/api/jobs/{job_id}`                                                                          | `required-unverified`                                  | Potwierdzenie parity statusów (`queued/running/done/error/cancelled/interrupted`).                          | Utrzymać legacy-api client.                                     |

## Runtime — potwierdzenie

- **Ta tura**: żaden powyższy ekran nie został przełączony. GUI runtime nadal korzysta z legacy Supabase i legacy `API_BASE_URL`.
- **Ubuntu API klient**: obecny w repo (`src/server/ubuntu-api.server.ts`), ale wywoływany **wyłącznie** przez `probeUbuntuApi()` w `/api/health` — read-only i tylko wtedy, gdy envy są ustawione.
- **Brak mock fallbacków** w produkcyjnym runtime.
